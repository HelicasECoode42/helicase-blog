# HELICASE 完全体开发契约

## 完成标准

“完全体”指网站已经能够长期、安全地承担个人数字展厅与公开写作入口，而不是堆满所有可能功能。交付必须同时满足：

1. 首页在桌面、平板、手机上层级清楚，Writings 不依赖内容数量撑起布局。
2. 新文章可创建、校验、预览、发布；发布后首页、归档、详情、前后篇导航和搜索一致更新。
3. 公开文章可按关键词、分类和标签发现。
4. 公开页面具备 canonical、Open Graph、结构化数据、RSS、sitemap 和 robots 基础设施。
5. Studio 与 Editor 不被搜索引擎收录；私密状态、Vault、密钥和本机数据不进入公开产物。
6. 外部 API 失败时核心内容仍可阅读；浏览器本地数据不会被误称为已发布数据。
7. `npm run verify`、关键页面实测、响应式与键盘操作验收全部通过。

## 查询与验证方案

每个阶段按以下证据链执行：

1. **静态查询**：用 `rg` 查找数据消费者、路由、外部资源、localStorage、网络请求和 noindex 边界。
2. **内容验证**：执行 `npm run check:content`，拒绝缺失摘要、非法分类、重复或不安全 frontmatter。
3. **构建验证**：执行 `npm run verify`，以生成路由清单和构建退出码为准。
4. **实页验证**：在本地浏览器检查 DOM、控制台、关键交互和 1280/768/390 三档布局。
5. **发布影响验证**：用一篇草稿夹具验证 draft 不公开，用公开夹具验证首页、归档、详情、搜索、RSS 和 sitemap。
6. **安全验证**：检查 `dist` 中不得出现 `.helicase`、私密路径、密钥、localhost Studio 数据或未授权个人文件。

## 安全边界

- 不读取、复制或发布工作区之外的私人 Vault、浏览器数据、令牌、Cookie 或密钥。
- 不自动把 Studio 的 `article`、`image` 捕获发布到公网；必须经过人工内容检查。
- 不删除或重写现有 Markdown、JSON、图片和 localStorage schema；迁移必须向后兼容并有备份方案。
- 不在公开客户端代码中放 API Key；需要密钥的能力只能使用 Cloudflare Secret/服务端代理。
- 不把用户输入直接拼进 HTML；动态内容使用文本节点或经过明确的转义/净化。
- 不执行破坏性 Git、文件删除、线上部署、域名/DNS 或权限变更，除非用户明确授权该具体动作。
- 外部依赖遵循最小化原则；能用构建期静态数据完成的功能不引入数据库或常驻后端。
- 每次修改保持小步可审计；构建失败时先修复当前阶段，不叠加下一阶段改动。

## 架构边界

- `src/content/blog/`：公开长文章的唯一事实源。
- `src/data/*.ts|json`：公开项目、活动、灵感和配置。
- `.helicase/`：仅本机 Studio 状态，禁止进入构建产物。
- `localStorage`：仅个人浏览器体验，不视为已发布、已同步或已备份。
- `worker/`：仅承载确需服务端保护的公开 API；`/api/*` 先于静态资产运行，但不得读取私人内容源。
