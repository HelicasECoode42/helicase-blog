type Link = { name: string; note: string; url: string };

const links: Link[] = [
  // Add only blogs you genuinely want to point toward.
  // { name: 'NAME', note: 'short fragment', url: 'https://example.com' },
];

export default links;
